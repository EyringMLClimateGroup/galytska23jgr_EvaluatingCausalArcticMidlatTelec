# Evgenia Galytska (EG), IUP, Bremen. 
# 06.10.2020
# Below I briefly provide major changes which I have ancountered in the recent version of Tigramite (as of 06.10.2020).  

(i)  pcmci = PCMCI( dataframe=dataframe, cond_ind_test=parcorr, verbosity=1)   # PCMCI does not accept var_names anymore

# slight changes in pcmci.get_lagged_dependencies, now the correct syntax is as follows:
(ii) correlations = pcmci.get_lagged_dependencies(tau_min=0, tau_max= max_timelag, val_only=True)['val_matrix']

(iii) pcmci.return_significant_links ... --> does not accept keyword argument p_matrix, instead pq_matrix should be used.

(iv) and there is warning message that return_significant_parents() is DEPRECATED and return_significant_links() should be used instead. 